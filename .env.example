# Apify Actor Local Service Configuration

# Server Configuration
PORT=3005
NODE_ENV=production

# Memory Configuration
# Memory limit for actor execution (in MB, default: 1024 = 1GB)
NODE_OPTIONS=--max-old-space-size=1024

# Playwright Configuration
# Skip browser download if using system Chromium
PLAYWRIGHT_SKIP_BROWSER_DOWNLOAD=1
# Path to system Chromium (for VPS deployment)
PLAYWRIGHT_CHROMIUM_EXECUTABLE_PATH=/usr/bin/chromium-browser

# Logging
LOG_LEVEL=info

# Optional: Enable debug logs
# DEBUG=*

# ============================================================================
# PROXY CONFIGURATION (Modular System)
# ============================================================================
# Escolha qual proxy usar: 'none' | 'iproyal' | 'zyte'
# - none: Sem proxy (conexão direta)
# - iproyal: IPRoyal residential proxies ($1.75-$7/GB)
# - zyte: Zyte Smart Proxy Manager ($0.06-$15.98/1000 requests)
PROXY_PROVIDER=none

# ----------------------------------------------------------------------------
# IPRoyal Residential Proxy
# ----------------------------------------------------------------------------
# Get 50% OFF with code: IPR50 at https://iproyal.com/
IPROYAL_ENABLED=false
IPROYAL_SERVER=geo.iproyal.com:32325
IPROYAL_USERNAME=customer-YOUR_USERNAME
IPROYAL_PASSWORD=your_password
IPROYAL_ROTATION=true
IPROYAL_SESSION_DURATION=600000  # 10 minutos em ms
IPROYAL_PRICE_PER_GB=1.75         # Para cálculo de custo estimado

# ----------------------------------------------------------------------------
# Zyte Smart Proxy Manager
# ----------------------------------------------------------------------------
# Get $5 free credit at https://app.zyte.com/o/signup
# Docs: https://docs.zyte.com/zyte-api/
ZYTE_ENABLED=false
ZYTE_API_KEY=your_zyte_api_key_here
ZYTE_STATIC_BYPASS=true  # Economiza 30-50% não usando proxy para assets estáticos
ZYTE_PROFILE=desktop     # 'desktop' ou 'mobile'
ZYTE_DEFAULT_TIER=1      # 1-5 (complexidade do site)
ZYTE_DEFAULT_REQUEST_TYPE=http  # 'http' ou 'browser'

# Optional: Playwright timeout overrides (in milliseconds)
# PLAYWRIGHT_TIMEOUT=30000
